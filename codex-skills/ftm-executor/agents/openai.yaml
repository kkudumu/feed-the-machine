interface:
  display_name: "FTM Executor"
  short_description: "Help with FTM Executor workflows"
  default_prompt: "Use $ftm-executor when you need help with its workflows."

policy:
  allow_implicit_invocation: true
