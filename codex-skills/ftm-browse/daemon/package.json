{
  "name": "ftm-browse",
  "version": "1.0.0",
  "description": "Headless browser daemon for Codex agents using Bun + Playwright",
  "type": "module",
  "scripts": {
    "start": "bun run server.ts",
    "cli": "bun run cli.ts",
    "build": "bun build --compile main.ts --outfile ../bin/ftm-browse --external electron --external chromium-bidi"
  },
  "dependencies": {
    "playwright": "^1.49.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
